-- ===========================================================================
-- Flyway Baseline Migration Template
-- ===========================================================================
-- CHANGE: Replace 'template' with your service schema name.
-- CHANGE: Add your service-specific tables, indexes, and constraints.
-- ===========================================================================

-- This migration file is version 1 — Flyway runs it on first deployment.
-- All subsequent schema changes get their own V{N}__{description}.sql file.

-- Example table (replace with actual service tables):
-- CREATE TABLE template.example (
--     id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
--     created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
--     updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
--     version INTEGER NOT NULL DEFAULT 0
-- );
