server:
  port: 8084

spring:
  application:
    name: svc-trip-catalog
  datasource:
    url: jdbc:postgresql://localhost:5432/novatrek_trips
    username: ${DB_USERNAME:novatrek}
    password: ${DB_PASSWORD:novatrek}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    com.novatrek.trips: INFO
    org.springframework.web: WARN
