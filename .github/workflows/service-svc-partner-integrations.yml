# ===========================================================================
# svc-partner-integrations — CI/CD Orchestrator
# ===========================================================================
# Builds, tests, scans, and deploys the partner integrations microservice.
# ===========================================================================

name: "svc-partner-integrations"

on:
  push:
    branches: [main]
    paths:
      - 'services/svc-partner-integrations/**'
  pull_request:
    paths:
      - 'services/svc-partner-integrations/**'

jobs:
  ci:
    uses: ./.github/workflows/service-ci.yml
    with:
      service-name: svc-partner-integrations
    secrets: inherit

  migrate-dev:
    needs: ci
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/db-migrate.yml
    with:
      service-name: svc-partner-integrations
      environment: dev
    secrets: inherit

  cd-dev:
    needs: [ci, migrate-dev]
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/service-cd.yml
    with:
      service-name: svc-partner-integrations
      environment: dev
      image-tag: ${{ needs.ci.outputs.image-tag }}
    secrets: inherit

  migrate-prod:
    needs: cd-dev
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/db-migrate.yml
    with:
      service-name: svc-partner-integrations
      environment: prod
    secrets: inherit

  cd-prod:
    needs: [ci, cd-dev, migrate-prod]
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/service-cd.yml
    with:
      service-name: svc-partner-integrations
      environment: prod
      image-tag: ${{ needs.ci.outputs.image-tag }}
    secrets: inherit
