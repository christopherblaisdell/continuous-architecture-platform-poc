name: "svc-guide-management"

on:
  push:
    branches: [main]
    paths:
      - 'services/svc-guide-management/**'
      - '.github/workflows/service-ci.yml'
      - '.github/workflows/service-svc-guide-management.yml'
  pull_request:
    paths:
      - 'services/svc-guide-management/**'
      - '.github/workflows/service-ci.yml'
      - '.github/workflows/service-svc-guide-management.yml'

jobs:
  ci:
    uses: ./.github/workflows/service-ci.yml
    with:
      service-name: svc-guide-management
    secrets: inherit
