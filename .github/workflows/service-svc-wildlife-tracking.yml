# ===========================================================================
# svc-wildlife-tracking — CI/CD Orchestrator
# ===========================================================================
# Builds, tests, scans, and deploys the wildlife tracking microservice.
# ===========================================================================

name: "svc-wildlife-tracking"

on:
  push:
    branches: [main]
    paths:
      - 'services/svc-wildlife-tracking/**'
  pull_request:
    paths:
      - 'services/svc-wildlife-tracking/**'

jobs:
  ci:
    uses: ./.github/workflows/service-ci.yml
    with:
      service-name: svc-wildlife-tracking
    secrets: inherit

  migrate-dev:
    needs: ci
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/db-migrate.yml
    with:
      service-name: svc-wildlife-tracking
      environment: dev
    secrets: inherit

  cd-dev:
    needs: [ci, migrate-dev]
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/service-cd.yml
    with:
      service-name: svc-wildlife-tracking
      environment: dev
      image-tag: ${{ needs.ci.outputs.image-tag }}
    secrets: inherit

  migrate-prod:
    needs: cd-dev
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/db-migrate.yml
    with:
      service-name: svc-wildlife-tracking
      environment: prod
    secrets: inherit

  cd-prod:
    needs: [ci, cd-dev, migrate-prod]
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/service-cd.yml
    with:
      service-name: svc-wildlife-tracking
      environment: prod
      image-tag: ${{ needs.ci.outputs.image-tag }}
    secrets: inherit
