name: "svc-loyalty-rewards"

on:
  push:
    branches: [main]
    paths:
      - 'services/svc-loyalty-rewards/**'
  pull_request:
    paths:
      - 'services/svc-loyalty-rewards/**'

jobs:
  ci:
    uses: ./.github/workflows/service-ci.yml
    with:
      service-name: svc-loyalty-rewards
    secrets: inherit

  cd-dev:
    needs: ci
    if: github.ref == 'refs/heads/main' && vars.DEPLOY_ENABLED == 'true'
    uses: ./.github/workflows/service-cd.yml
    with:
      service-name: svc-loyalty-rewards
      environment: dev
    secrets: inherit

  migrate-dev:
    needs: cd-dev
    if: github.ref == 'refs/heads/main' && vars.DEPLOY_ENABLED == 'true'
    uses: ./.github/workflows/db-migrate.yml
    with:
      service-name: svc-loyalty-rewards
      environment: dev
    secrets: inherit

  cd-prod:
    needs: [ci, cd-dev, migrate-dev]
    if: github.ref == 'refs/heads/main' && vars.DEPLOY_ENABLED == 'true'
    uses: ./.github/workflows/service-cd.yml
    with:
      service-name: svc-loyalty-rewards
      environment: prod
    secrets: inherit

  migrate-prod:
    needs: cd-prod
    if: github.ref == 'refs/heads/main' && vars.DEPLOY_ENABLED == 'true'
    uses: ./.github/workflows/db-migrate.yml
    with:
      service-name: svc-loyalty-rewards
      environment: prod
    secrets: inherit
