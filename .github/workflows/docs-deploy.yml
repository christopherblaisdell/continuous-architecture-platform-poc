# ===========================================================================
# Deploy Documentation to Azure Static Web Apps
# ===========================================================================
# Triggers:
#   - Push to main: builds and deploys to production
#   - Pull request to main: builds and deploys to staging preview
# ===========================================================================

name: Deploy Documentation

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'requirements-docs.txt'
      - 'decisions/**'
      - 'services/**'
      - 'roadmap/**'
      - 'research/**'
      - 'phase-*/**'
      - 'portal/docs/specs/**'
      - 'portal/docs/metadata/**'
      - 'portal/docs/events/**'
      - 'portal/scripts/**'
      - 'portal/mkdocs.yml'
      - 'portal/staticwebapp.config.json'
      - 'README.md'
      - 'CLOSING-THE-LOOP.md'
      - 'staticwebapp.config.json'
      - '.github/workflows/docs-deploy.yml'
  pull_request:
    branches: [main]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'
      - 'requirements-docs.txt'
      - 'decisions/**'
      - 'services/**'
      - 'roadmap/**'
      - 'research/**'
      - 'phase-*/**'
      - 'portal/docs/specs/**'
      - 'portal/docs/metadata/**'
      - 'portal/docs/events/**'
      - 'portal/scripts/**'
      - 'portal/mkdocs.yml'
      - 'portal/staticwebapp.config.json'
      - 'README.md'
      - 'CLOSING-THE-LOOP.md'
      - 'staticwebapp.config.json'

permissions:
  contents: read

jobs:
  # -------------------------------------------------------------------------
  # Build MkDocs site
  # -------------------------------------------------------------------------
  build:
    runs-on: ubuntu-latest
    name: Build Documentation
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history required for git-revision-date plugin

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: requirements-docs.txt

      - name: Install dependencies
        run: pip install -r requirements-docs.txt

      - name: Install PlantUML
        run: sudo apt-get update && sudo apt-get install -y plantuml

      - name: Generate artifacts and build site
        run: bash portal/scripts/generate-all.sh

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: docs-site
          path: portal/site/
          retention-days: 1

  # -------------------------------------------------------------------------
  # Deploy to Azure Static Web Apps
  # -------------------------------------------------------------------------
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Azure
    needs: build
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    environment:
      name: ${{ github.event_name == 'push' && 'production' || 'staging' }}
      url: ${{ steps.deploy.outputs.static_web_app_url }}
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: docs-site
          path: site/

      - name: Deploy to Azure Static Web Apps
        id: deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: site
          skip_app_build: true
          # For PRs, Azure SWA automatically creates a staging environment

  # -------------------------------------------------------------------------
  # Close staging environment when PR is closed
  # -------------------------------------------------------------------------
  close-staging:
    runs-on: ubuntu-latest
    name: Close Staging Environment
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    steps:
      - name: Close staging environment
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: close
