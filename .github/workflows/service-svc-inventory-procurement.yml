# ===========================================================================
# svc-inventory-procurement — CI/CD Orchestrator
# ===========================================================================
# Builds, tests, scans, and deploys the inventory procurement microservice.
# ===========================================================================

name: "svc-inventory-procurement"

on:
  push:
    branches: [main]
    paths:
      - 'services/svc-inventory-procurement/**'
  pull_request:
    paths:
      - 'services/svc-inventory-procurement/**'

jobs:
  ci:
    uses: ./.github/workflows/service-ci.yml
    with:
      service-name: svc-inventory-procurement
    secrets: inherit

  migrate-dev:
    needs: ci
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/db-migrate.yml
    with:
      service-name: svc-inventory-procurement
      environment: dev
    secrets: inherit

  cd-dev:
    needs: [ci, migrate-dev]
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/service-cd.yml
    with:
      service-name: svc-inventory-procurement
      environment: dev
      image-tag: ${{ needs.ci.outputs.image-tag }}
    secrets: inherit

  migrate-prod:
    needs: cd-dev
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/db-migrate.yml
    with:
      service-name: svc-inventory-procurement
      environment: prod
    secrets: inherit

  cd-prod:
    needs: [ci, cd-dev, migrate-prod]
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/service-cd.yml
    with:
      service-name: svc-inventory-procurement
      environment: prod
      image-tag: ${{ needs.ci.outputs.image-tag }}
    secrets: inherit
